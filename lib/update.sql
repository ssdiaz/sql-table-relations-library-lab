-- SELECT id
-- AS last_id 
-- FROM characters
-- ORDER BY id DESC 
-- LIMIT 1;

-- UPDATE characters SET species = "Martians" WHERE id = last_id;

UPDATE characters SET species = "Martian" WHERE id = 8;